# AWS Static Website Project (S3 + CloudFront + Terraform)

This project demonstrates how to host a static website securely on **Amazon S3** and distribute it using **CloudFront** — all within the AWS Free Tier.

## Includes
- AWS CLI automation (`deploy_static_site.sh`)
- Terraform Infrastructure as Code setup (`terraform-static-site/`)
- Cleanup scripts to remove all resources safely

## Run
```bash
bash deploy_static_site.sh
